---
- hosts: all
  remote_user: vagrant
  sudo: True
  roles:
      - common
      - database
      - mq
      - glance
      - keystone
      - nova-controller
      - neutron-controller
      - dashboard
      - cinder-controller
