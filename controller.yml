---
- hosts: all
  remote_user: vagrant
  sudo: True
  roles:
      - common
      - database
      - mq
      - glance
      - keystone
      - nova-controller
      - neutron-common
      - neutron-server
      - dashboard
      - cinder-controller
